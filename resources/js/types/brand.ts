export default interface Brand {
    id: string;
    name: string;
    image_url: string;
    slug: string;
    featured: boolean;
    description?: string;
    created_at: string;
    updated_at: string;
};
